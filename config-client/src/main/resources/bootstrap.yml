spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8030/

server:
  port: 8040
  servlet:
    context-path: /springCloud